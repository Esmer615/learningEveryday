# 《实时渲染》第6章纹理相关的阅读
纹理的获取过程，坐标是如何转换的
像素分辨率高于纹素：各种滤波插值来计算，比如双线性插值
一个像素单元里面包括多个纹素：mipmap，SAT加和计算面积，各项异性mipmap
立方体贴图，一些纹理映射的方法，比如球体映射、平面映射、圆柱体映射等
三维的纹素，第三维可能会表示深度值这些，比如医学上的扫描图像
纹理表示：纹理图集，即一个图里面多张纹理放一起避免切换开销；纹理数组，通过数组的形式每一位存一张图
纹理压缩：最基本的BC分块压缩，但是有损失，其有很多变体
程序化纹理：这个没有说太多
凹凸映射、法线贴图
视差映射、浮雕映射（视差遮挡映射）